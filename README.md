# D-42 OMNI - Галерея контента с Firebase

**D-42 OMNI** — это легковесное, адаптивное веб-приложение для просмотра, поиска и управления медиаконтентом (изображениями и видео) с использованием внешнего API (Rule34.xxx) и облачной базы данных (Google Firebase) для хранения пользовательских данных, таких как избранное и чёрные списки.

Приложение спроектировано с акцентом на мобильный пользовательский опыт (Mobile-First) и использует минималистичный стек: чистый HTML, ванильный JavaScript и фреймворк [Tailwind CSS](https://tailwindcss.com/) для стилизации.

## 🚀 Основные возможности

* **Динамический поиск:** Поиск контента по тегам с использованием внешнего API.
* **Адаптивная сетка (Masonry):** Отображение контента в оптимизированной для мобильных устройств сетке.
* **Бесконечный скролл:** Автоматическая подгрузка новых постов при прокрутке.
* **Управление избранным (Firebase):** Аутентификация анонимного пользователя и сохранение избранных постов в коллекции Firestore.
* **Чёрный список (Blacklist):** Фильтрация контента по тегам, которые пользователь не хочет видеть (хранится в Firebase).
* **Кастомный видеоплеер:** Встроенный HTML5-плеер с кастомными контролами, прогресс-баром и поддержкой мобильных жестов.
* **Переключение темы:** Поддержка светлой и тёмной темы.
* **Просмотр деталей:** Модальное окно с метаданными (ID, Score, теги, автор) и функцией скачивания.
* **Навигация жестами:** Свайпы влево/вправо в модальном окне для переключения постов.
* **"Мне повезет" (Lucky):** Случайный поиск по популярным тегам.

## ⚙️ Стек технологий

* **Frontend:** HTML5, Ванильный JavaScript (ESM).
* **Стилизация:** [Tailwind CSS](https://tailwindcss.com/) (через CDN).
* **Иконки:** [Lucide Icons](https://lucide.dev/) (через CDN).
* **База данных/Аутентификация:** Google Firebase (Firestore, Auth) — используется CDN-версия SDK v11.6.1.
* **API:** Rule34.xxx (используется `dapi` для получения данных в формате XML).

## 🛠️ Настройка и развертывание

### 1. Настройка API-ключа

Для работы с внешним API необходимо указать приватные данные в начале `<script>` блока:

```javascript
// --- КОНФИГУРАЦИЯ ---
const API_KEY = 'YOUR_API_KEY'; // Ключ API для доступа к Rule34 DAPI
const USER_ID = 'YOUR_USER_ID'; // Ваш ID пользователя на Rule34
const BASE_URL = `https://api.rule34.xxx/index.php?page=dapi&s=post&q=index&api_key=${API_KEY}&user_id=${USER_ID}`;
